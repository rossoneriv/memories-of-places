const memoryList = class MemoryList {

    constructor(props) {
        let {lat, lon, place, memDate, memContents} = props;
        this.lat = lat;
        this.lon = lon;
        this.place = place;
        this.memDate = memDate;
        this.memContents = memContents; 
        debugger;
    }

    init(){
        debugger;
    }
}